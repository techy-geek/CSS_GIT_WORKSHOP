#include <stdio.h>

int factorial(int n) {
    // write the function to calculate the factorial of n

}

int main() {
    int n; 
    scanf("%d", &n);
    int fact = factorial(n);
    printf("Factorial of %d is %d\n", n, fact);
    return 0;
}